<%= render("/shared/data/all/list/list_header", objects: @sounds) -%>

<table class="tablesorter">
  <thead>
  <tr>
    <%= fancy_th_tag(name: 'Updated by', group: 'housekeeping') -%>
    <%= fancy_th_tag(name: 'Last updated', group: 'housekeeping') -%>
  </tr>
  </thead>

  <!-- todo @mjy Do you want all these fields displayed in the list? -->

  <tbody>
  <% @images.each do |sound| %>
      <%= content_tag(:tr, class: [:contextMenuCells, :btn, 'btn-neutral']) do -%>
          <%= fancy_metadata_cells_tag(sound) -%>
      <% end %>
  <% end %>
  </tbody>
</table>
